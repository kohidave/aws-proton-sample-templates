name: My Workflow
on:
  pull_request:
    branches: [ "main" ]
jobs:
  TemplateChecker:
    runs-on: ubuntu-latest 
    steps:
      - name: Run action
        # Put your action repo here
        uses: aws-samples/aws-proton-template-checker-action@main
      
